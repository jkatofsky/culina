{
  "name": "culina",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "client": "cd client && yarn start",
    "start": "node server.js",
    "deploy": "cd client && yarn build && cd .. && gcloud app deploy"
  },
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "socket.io": "^4.4.1"
  }
}
